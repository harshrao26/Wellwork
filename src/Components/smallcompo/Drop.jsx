import React from "react";
import { FaChevronDown } from "react-icons/fa";

const Drop = () => {
  return (
    <FaChevronDown size={20} color="#00000099" style={{ marginRight: 20 }} />
    // <img alt ='' src={drop} style={{ paddingRight: '15px', cursor: "pointer", pointerEvents: 'none',position:'absolute',right:'0' }} />)
  );
};

export default Drop;
